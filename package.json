{
  "name": "jsonc-pragma",
  "version": "1.0.2",
  "description": "A simple, yet powerful pragma analyzer and utility library for JSON with Comments",
  "main": "dist/main.js",
  "source": "src/main.ts",
  "license": "MIT",
  "scripts": {
    "build": "run clean && microbundle --target node --no-sourcemap --compress -f cjs && yarn types",
    "watch": "run clean && microbundle watch --target node -f cjs",
    "clean": "shx rm -rf dist",
    "types": "tsc -p . -m amd -d --out types/main.js && shx cp types/main.d.ts dist && shx rm -rf types && node scripts/postprocess.js",
    "test": "jest"
  },
  "dependencies": {
    "match-bracket": "^1.0.0"
  },
  "devDependencies": {
    "@types/jest": "^24.9.0",
    "@types/node": "^13.1.7",
    "codecov": "^3.6.1",
    "jest": "^24.9.0",
    "microbundle": "^0.12.0-next.7",
    "prettier": "^1.19.1",
    "run-script-proxy": "^1.0.1",
    "shx": "^0.3.2",
    "ts-jest": "^24.3.0",
    "tslib": "^1.10.0",
    "tslint": "^5.20.1",
    "tslint-config-airbnb": "^5.11.2",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.7.4"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/arnohovhannisyan/jsonc-pragma"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "moduleNameMapper": {
      "^~/(.*)": "<rootDir>/src/$1"
    }
  }
}
